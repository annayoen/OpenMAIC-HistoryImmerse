The user said: {{userMessage}}

Now generate a COMPLETE new round of dialogue in JSONL format (one JSON object per line, no array wrapper, no commas between lines).

Your output MUST contain at least 4 lines:
1. EVERY historical character speaks (at least 2 lines, 2-5 rounds)
2. Commentator analysis (exactly 1 line)
3. Guide summary (exactly 1 line)

DO NOT output only the guide's message. That is WRONG.
DO NOT skip the commentator. That is WRONG.
DO NOT let any character remain silent. That is WRONG.
DO NOT wrap output in a JSON array []. That is WRONG.

All characters must directly address and respond to the user's question or viewpoint.